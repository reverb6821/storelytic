{
  "name": "server",
  "version": "1.0.0",
  "description": "TODOapp webserver",
  "main": "./src/app.js",
  "repository": "https://github.com/reverb6821/todo-app",
  "author": "Giuseppe Gigliotti",
  "license": "MIT",
  "scripts": {
    "start": "nodemon ./src/app.js",
    "db:migrate": "yarn sequelize-cli db:migrate",
    "db:migrate:debug": "yarn sequelize-cli db:migrate --debug",
    "db:migrate:undo": "yarn sequelize-cli db:migrate:undo",
    "db:seed:all": "yarn sequelize-cli db:seed:all",
    "db:seed:all:debug": "yarn sequelize-cli db:seed:all --debug",
    "db:seed:undo": "yarn sequelize-cli db:seed:undo"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "sequelize-cli": "^6.6.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-session": "^2.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "morgan": "^1.10.0",
    "sequelize": "^6.28.0",
    "sqlite3": "^5.1.4",
    "uuid": "^9.0.0",
    "winston": "^3.8.2"
  }
}
